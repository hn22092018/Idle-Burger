using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheatGroup : MonoBehaviour
{
   public void CheatTime() {
        //if (Application.platform != RuntimePlatform.Android && Application.platform != RuntimePlatform.IPhonePlayer && Application.platform != RuntimePlatform.WebGLPlayer)
        //    GameManager.instance.CheatTime();
    }
    public void CheatCash() {
        //if (Application.platform != RuntimePlatform.Android && Application.platform != RuntimePlatform.IPhonePlayer && Application.platform != RuntimePlatform.WebGLPlayer)
        //    GameManager.instance.AddCash(10000000);
    }
}
